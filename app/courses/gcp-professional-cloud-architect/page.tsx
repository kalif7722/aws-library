import { gcpCourses } from "../../gcp-course-data";
import { gcpServices } from "../../gcp-data";
import "../../components/GcpLibrary.css";
export default function GcpProfessionalCloudArchitectCourse() { const course = gcpCourses[0]; return <main className="workspace gcp-workspace"><nav className="top-nav" aria-label="Primary navigation"><a className="brand-link" href="/">Visual Learning</a><div><a className="home-button" href="/">Home</a><a href="/gcp-services">GCP services</a><a className="active" href="/courses/gcp-professional-cloud-architect">GCP PCA</a></div></nav><header className="gcp-heading"><div><p className="course-kicker">Google Cloud · {course.level}</p><h1>{course.title}</h1><p>{course.description}</p></div><a href={course.sourceUrl} target="_blank" rel="noreferrer">Official certification guide ↗</a></header><section className="gcp-content-card"><h2>Course service map</h2><p>This initial course map establishes the shared navigation pattern. Each service opens its GCP guide, visual, walkthrough, related services, and official sources.</p><div className="gcp-related">{course.services.map(name => { const service = gcpServices.find(item => item.displayName === name); return service ? <a key={service.slug} href={`/gcp-services?service=${service.slug}`}>{service.displayName}</a> : null; })}</div></section><section className="gcp-content-card"><h2>Coverage status</h2><ul><li>Initial architecture service scope mapped.</li><li>EL10 visuals are generated in batches and require reference-style review.</li><li>Walkthroughs will be added as paired primary/companion assets.</li></ul></section></main>; }
